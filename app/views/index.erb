<div class="container">
  <% if session[:user_id] %>
    <% @users.each do |user| %>
      <%= user.name %>
      <%= user.email %>
  <% else %>
    <%= "<h1>Welcome</h1>" %>
  <% end %>
  <%# TODO:
  1. If user is not signed in - display "Welcome".
  2. If user is signed in then  display all users %>
</div>
